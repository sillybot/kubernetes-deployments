apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testapplication.fullname" . }}-test-connection"
  labels:
    {{- include "testapplication.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "testapplication.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
